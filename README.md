# NetFusion
This is a personal project tool that is supposed to be similar to Traditional NetCat without the network scanning ability.


This information is in the notes.md as well

There is not a required.txt file yet because I am still learning how all this works

Here are the required packeges needed for this scirpt to work properly:
import os
import sys
import pty
import socket
import getopt
import threading
import subprocess
import select
import psutil

Keep in mind that some of these imports will change over time and some of them are planned on being used for future features
That are currently in progress on my end
Keep an eye on this for now for any changes on the packages being used
